{
  "name": "api-services",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo --ui stream run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "start": "turbo --ui stream run start"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
